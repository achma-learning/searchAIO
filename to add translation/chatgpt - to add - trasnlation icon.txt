Sure! To achieve the functionality you want, here’s how we can break it down:

### Key Requirements:

1. **Circular Icon with World Map**: Place a circular icon next to the "site:" checkbox, showing a world map.
2. **Pop-up to Select Output Language**: When the icon is clicked, a pop-up should appear to select a language (English, French, Arabic, Chinese, Spanish, Russian, or "original" as the default).
3. **Close Pop-up with Escape**: The pop-up should be closed when the **Escape** key is pressed.
4. **Update Icon to Show Language Code**: After selecting a language, the icon should update to show the corresponding language code (e.g., "en", "fr", etc.).
5. **World Map Icon for Default Language**: When "original language" is selected (or no language selected), the world map icon should appear.

### Implementation Plan

We'll need to:

* Add the circular world map icon.
* Implement a pop-up for selecting a language.
* Change the icon to show the selected language code (or the world map when original is selected).
* Ensure the pop-up can be closed with the **Escape** key.

Here’s how you can achieve this in your `index.html`:

### Full HTML & JavaScript Implementation

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SearchAIO</title>
    <link rel="stylesheet" href="style.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="script.js"></script>
    <style>
        /* Styles for the search bar, pop-up, and language selection */
        #searchWrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 20px;
        }

        #langSelector {
            padding: 8px;
            font-size: 14px;
            margin-left: 10px;
        }

        /* Circular world map icon */
        #langIcon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #4285f4;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            margin-left: 10px;
            color: white;
            font-size: 20px;
            text-align: center;
        }

        /* Pop-up for language selection */
        #langPopup {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 9999;
            display: none;
            justify-content: center;
            align-items: center;
        }

        #langPopupContent {
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
            max-width: 400px;
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .langBtn {
            padding: 10px 20px;
            background: #4285f4;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            margin: 5px;
        }

        .langBtn:hover {
            background: #3367d6;
        }

        /* Hide the world map icon when a language is selected */
        #langIcon.languageSelected {
            background-color: transparent;
            color: #4285f4;
            font-size: 18px;
        }
    </style>
</head>
<body>

    <!-- Search Bar and Language Dropdown -->
    <div id="searchWrapper">
        <input type="text" id="searchInput" placeholder="Search..." style="padding: 8px; font-size: 16px;">
        <select id="langSelector">
            <option value="original">Original</option>
            <option value="en">English</option>
            <option value="fr">French</option>
            <option value="ar">Arabic</option>
            <option value="zh">Chinese</option>
            <option value="es">Spanish</option>
            <option value="ru">Russian</option>
        </select>
        <div id="langIcon">🌐</div>
    </div>

    <!-- Language Popup -->
    <div id="langPopup">
        <div id="langPopupContent">
            <h3>Select Output Language</h3>
            <button class="langBtn" data-lang="en">English (en)</button>
            <button class="langBtn" data-lang="fr">French (fr)</button>
            <button class="langBtn" data-lang="ar">Arabic (ar)</button>
            <button class="langBtn" data-lang="zh">Chinese (zh)</button>
            <button class="langBtn" data-lang="es">Spanish (es)</button>
            <button class="langBtn" data-lang="ru">Russian (ru)</button>
            <button class="langBtn" data-lang="original">Original</button>
        </div>
    </div>

    <script>
        // References to elements
        const langIcon = document.getElementById('langIcon');
        const langPopup = document.getElementById('langPopup');
        const langPopupContent = document.getElementById('langPopupContent');
        const langSelector = document.getElementById('langSelector');
        const searchInput = document.getElementById('searchInput');

        // Open pop-up when the world map icon is clicked
        langIcon.addEventListener('click', function() {
            langPopup.style.display = 'flex';
        });

        // Close pop-up with Escape key
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                langPopup.style.display = 'none';
            }
        });

        // Close pop-up if any language button is clicked
        langPopupContent.addEventListener('click', function(event) {
            if (event.target.classList.contains('langBtn')) {
                const selectedLang = event.target.getAttribute('data-lang');
                if (selectedLang === 'original') {
                    langIcon.innerHTML = '🌐';
                    langIcon.classList.remove('languageSelected');
                    langSelector.value = 'original'; // Reset dropdown to original
                } else {
                    langIcon.innerHTML = selectedLang.toUpperCase();
                    langIcon.classList.add('languageSelected');
                    langSelector.value = selectedLang; // Update dropdown with the selected language
                }
                langPopup.style.display = 'none';
            }
        });

        // Update the search based on selected language from the dropdown
        langSelector.addEventListener('change', function() {
            const selectedLang = langSelector.value;
            if (selectedLang === 'original') {
                langIcon.innerHTML = '🌐'; // World map icon when original is selected
                langIcon.classList.remove('languageSelected');
            } else {
                langIcon.innerHTML = selectedLang.toUpperCase(); // Display selected language code
                langIcon.classList.add('languageSelected');
            }
        });

        // Optionally, trigger search when Enter is pressed in the search bar
        searchInput.addEventListener('keypress', function(event) {
            if (event.key === 'Enter') {
                const langCode = langSelector.value;
                const searchText = searchInput.value;
                
                if (searchText.trim()) {
                    const searchURL = `https://translate.google.com/translate?sl=auto&tl=${langCode}&u=${encodeURIComponent('https://www.google.com/search?q=' + searchText)}`;
                    window.open(searchURL, '_blank');
                }
            }
        });
    </script>

</body>
</html>
```

### Breakdown of the Features:

1. **Circular Icon with World Map**:

   * The world map icon (`🌐`) is initially shown inside a circular div (`#langIcon`).
   * When the language is selected, the icon is replaced with the corresponding language code (e.g., `EN`, `FR`).

2. **Pop-up for Language Selection**:

   * When the user clicks the world map icon (`#langIcon`), a pop-up (`#langPopup`) appears with buttons to select the output language.
   * The user can select one of the languages (English, French, Arabic, Chinese, Spanish, Russian, or "Original").

3. **Escape Key to Close Pop-up**:

   * The pop-up can be closed by pressing the **Escape** key (`keydown` event listener).

4. **Updating the Icon**:

   * When the user selects a language, the world map icon is replaced with the language code (e.g., `en`, `fr`, etc.).
   * The icon shows the world map again when "Original" is selected.

### How It Works:

* **Click on the icon**: The world map icon (`🌐`) opens a language selection pop-up.
* **Choose a language**: Selecting a language updates the icon to the corresponding language code (e.g., `EN` for English).
* **Press Escape**: Closes the language selection pop-up.
* **Search Bar Interaction**: The search works as usual, with the selected language affecting the search's language
